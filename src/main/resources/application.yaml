spring:
  application:
    name: rpp-enroll-training

  datasource:
    url: jdbc:postgresql://localhost:5432/rpp
    username: postgres
    password: Pedro2012=
    driver-class-name: org.postgresql.Driver

  rabbitmq:
    listener:
      simple:
        default-requeue-rejected: false
    host: localhost
    port: 5672
    username: guest
    password: guest

  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080